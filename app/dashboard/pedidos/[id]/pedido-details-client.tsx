'use client';
import Link from 'next/link'; import { useState } from 'react'; import { createClient } from '@/app/lib/supabase/client';
const labels:Record<string,string>={pendente:'Pendente',confirmado:'Aceito',em_separacao:'Em separação',pronto:'Pronto',saiu_para_entrega:'Enviado',entregue:'Entregue',cancelado:'Cancelado'};
export default function PedidoDetailsClient({pedido:initial,itens,pixKey,pixName}:{pedido:any;itens:any[];pixKey:string|null;pixName:string|null}){const[pedido,setPedido]=useState(initial);const[loading,setLoading]=useState(false);const[error,setError]=useState('');const action=pedido.status==='pendente'?['confirmado','Aceitar pedido','confirmado_em']:pedido.status==='confirmado'||pedido.status==='em_separacao'||pedido.status==='pronto'?['saiu_para_entrega','Enviar pedido','saiu_entrega_em']:pedido.status==='saiu_para_entrega'?['entregue','Marcar como entregue','entregue_em']:null;async function update(){if(!action)return;setLoading(true);const{error}=await createClient().from('pedidos').update({status:action[0], [action[2]]:new Date().toISOString(),updated_at:new Date().toISOString()}).eq('id',pedido.id);setLoading(false);if(error)return setError(error.message);setPedido({...pedido,status:action[0]})}return <div className="space-y-6"><Link href="/dashboard/pedidos" className="text-sm font-semibold text-zinc-500">← Pedidos</Link><header className="flex flex-wrap items-center justify-between gap-4 rounded-3xl bg-white p-6 shadow-sm"><div><p className="text-sm text-zinc-500">Pedido #{pedido.numero_pedido}</p><h1 className="text-3xl font-bold">{labels[pedido.status]||pedido.status}</h1></div>{action&&<button onClick={update} disabled={loading} className="rounded-full bg-zinc-950 px-5 py-3 text-sm font-bold text-white">{loading?'Salvando...':action[1]}</button>}</header>{error&&<p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="grid gap-6 lg:grid-cols-2"><section className="rounded-3xl bg-white p-6 shadow-sm"><h2 className="font-bold">Itens</h2>{itens.map(item=><div key={item.id} className="mt-4 flex justify-between border-t pt-4 text-sm"><span>{item.quantidade}× {item.produto_nome}</span><b>{money(item.subtotal)}</b></div>)}<div className="mt-5 border-t pt-4 text-lg font-bold">Total: {money(pedido.valor_total)}</div></section><section className="rounded-3xl bg-white p-6 shadow-sm"><h2 className="font-bold">Cliente e entrega</h2><p className="mt-3">{pedido.cliente_nome}</p><p className="text-sm text-zinc-500">{pedido.cliente_telefone}</p><p className="mt-4 text-sm">{pedido.tipo_entrega==='retirada'?'Retirada no mercado':`${pedido.endereco_rua}, ${pedido.endereco_numero} — ${pedido.endereco_bairro}, ${pedido.endereco_cidade}/${pedido.endereco_estado}`}</p><h2 className="mt-6 font-bold">Pagamento</h2><p className="mt-2 text-sm capitalize">{pedido.forma_pagamento}</p>{pedido.forma_pagamento==='pix'&&<p className="mt-2 text-sm text-zinc-500">{pixName}<br/>{pixKey}</p>}</section></div></div>}
function money(value:any){return new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(value||0))}
